#!/usr/bin/env bash
# One-time setup script — already used, kept for reference only.
# To push future changes: git add . && git commit -m "..." && git push
echo "Repo already initialised. Use: git add . && git commit -m 'msg' && git push"
