# ZeroCode Career Coach — core dependencies
# Works locally (Ollama) and on Streamlit Community Cloud (hosted model).
# For voice features (local use only), also run: pip install -r requirements-voice.txt

# UI
streamlit>=1.36

# Data validation
pydantic>=2.7

# LLM clients
requests>=2.31          # Ollama REST + hosted fallback (Groq/OpenRouter)

# Document parsing
pdfplumber>=0.11
python-docx>=1.1

# Match scoring
rapidfuzz>=3.9
numpy>=1.26

# Templating (single-file HTML dossier)
Jinja2>=3.1
markupsafe>=2.1

# Dev / tests
pytest>=8.0
